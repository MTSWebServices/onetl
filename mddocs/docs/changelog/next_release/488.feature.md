Introduce new `FTP.Extra` and `FTPS.Extra` classes:
```python
FTP(
  extra=FTP.Extra(
    encoding="utf-8",
    use_passive_mode=True,
  ),
)
```

This new class allows passing additional params directly to `ftputil.session.session_factory`.
