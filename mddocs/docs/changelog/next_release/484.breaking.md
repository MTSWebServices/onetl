Replace `HDFS(timeout=..., retry=...)` with new class:
```python
HDFS(
  extra=HDFS.Extra(
    timeout=...,
    retry=...,
  ),
)
```

This new class allows passing additional params directly to `hdfs.client.Client`.
