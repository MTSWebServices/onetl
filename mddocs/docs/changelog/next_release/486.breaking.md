Replace `WebDAV(ssl_verify=...)` with new class:
```python
WebDAV(
  extra=WebDAV.Extra(
    timeout=...,
    retry=...,
    ssl_verify=...,
  ),
)
```

This new class allows passing additional params directly to `webdav3.client.Client`.
