Replace `S3(ssl_verify=...)` with new class:
```python
S3(
  extra=S3.Extra(
    timeout=...,
    retry=...,
    ssl_verify=...,
  ),
)
```

This new class allows passing additional params directly to `minio.Minio` client.
