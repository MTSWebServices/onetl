Introduce new `Samba.Extra` class:
```python
Samba(
  extra=Samba.Extra(
    connection_timeout=...,
    operation_timeout=...,
    my_name=...,
    sign_options=...,
  )
)
```

This new class allows passing additional params directly to `smb.SMBConnection.smb.SMBConnection` class.
